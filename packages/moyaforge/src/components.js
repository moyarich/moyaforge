/**
 * Built-in MDX components provided by MoyaForge.
 *
 * This object is intentionally plain so consumers can spread it into the
 * standard MDX provider component map and override any key.
 */
export const moyaForgeComponents = Object.freeze({});
